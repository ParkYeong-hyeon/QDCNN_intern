# Experiment 0001: Baseline Reproduction Configuration

# Inherit from default config
_base_: "../config/default.yaml"

# Experiment-specific overrides
experiment:
  name: "0001_baseline_repro"
  description: "Baseline reproduction"

# Model
model:
  name: "baseline"

# Training
training:
  epochs: 100


