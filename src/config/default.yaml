# Default configuration

# Data
data:
  root: "data/"
  batch_size: 32
  num_workers: 4

# Model
model:
  name: "baseline"
  params: {}

# Training
training:
  epochs: 100
  learning_rate: 0.001
  optimizer: "adam"
  device: "cuda"

# Logging
logging:
  log_dir: "logs/"
  log_interval: 10

